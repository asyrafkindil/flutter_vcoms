class Product {
  int id;
  String name;
  String description;
  double price;
  int categoryId;
  String photoPath;

  Product({this.id, this.name, this.description, this.price, this.categoryId, this.photoPath});
}